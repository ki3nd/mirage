PROMPT = """
ChromaDB is mounted as a read-only documentation filesystem.
Use ls, find, tree, grep, cat, head, tail, and search to explore pages.

Use search for semantic or scoped retrieval:

search "<query>" [path ...] [--method semantic|keyword|hybrid] [--top-k N] [--threshold F]
"""
